// app.component.ts
import { Component } from '@angular/core';
import { CarService } from './car.service';

@Component({
    selector: 'app-root',
    template: `...`,
    styles: [
        `
      .car-item {
        margin-bottom: 20px;
      }
    `,
    ],
})
export class AppComponent {
    // ... залиште інший код без змін
}
